const defaultState = {
    pos: [0, 0]
}

const knightReducer = (state = defaultState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default knightReducer;